function route(){
  var routing_tables = {}
    var match = function(req){
          return routing_tables[req.url]

    }

    var get = function(path,handler){
      routing_tables[path] = handler;
    }
return {match : match,get : get};
}

module.exports = route();
